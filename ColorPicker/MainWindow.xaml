<controls:MicaWindow
    x:Class="ColorPicker.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:MicaWPF.Lite.Controls;assembly=MicaWPF.Lite"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:lang="clr-namespace:ColorPicker.Properties"
    xmlns:local="clr-namespace:ColorPicker"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:tb="http://www.hardcodet.net/taskbar"
    Title="{x:Static lang:Resources.ColorPickerMax}"
    Width="950"
    Height="600"
    MinWidth="500"
    MinHeight="500"
    FontFamily="/Fonts/#Hauora"
    Foreground="{DynamicResource Foreground1}"
    Icon="Images/CPM.png"
    ResizeMode="CanResize"
    WindowStartupLocation="CenterScreen"
    mc:Ignorable="d">
    <controls:MicaWindow.Resources>
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Height" Value="32" />
            <Setter Property="WindowChrome.IsHitTestVisibleInChrome" Value="True" />
            <Setter Property="IsTabStop" Value="False" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            CornerRadius="5"
                            SnapsToDevicePixels="true">
                            <ContentPresenter
                                x:Name="contentPresenter"
                                Margin="0"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Focusable="False"
                                RecognizesAccessKey="True" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource WindowButtonsHover1}" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource WindowButtonsPressed1}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Flat icon-only RadioButton for nav bar Row 0 (主页 / 书签 / 设置) -->
        <Style x:Key="NavIconButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource Foreground1}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="8"
                            Padding="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource CardBackground}" />
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource LightAccentColor}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Flat icon-only Button for nav bar Row 1 (快捷操作) -->
        <Style x:Key="NavActionButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource AccentColor}" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Width" Value="40" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="8"
                            Padding="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource LightAccentColor}" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </controls:MicaWindow.Resources>

    <controls:MicaWindow.TitleBarContent>
        <StackPanel
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Orientation="Horizontal">
            <Border
                Grid.Column="1"
                Padding="5 2"
                VerticalAlignment="Center"
                Background="{DynamicResource CardBackground}"
                CornerRadius="10"
                WindowChrome.IsHitTestVisibleInChrome="False">
                <TextBlock
                    x:Name="VersionTxt"
                    d:Text="5.4.0.2308"
                    FontSize="10"
                    FontWeight="SemiBold"
                    Foreground="{DynamicResource DarkGray}" />
            </Border>
            <Button
                x:Name="PinBtn"
                Grid.Column="2"
                Width="46"
                Height="24"
                Background="Transparent"
                Click="PinBtn_Click"
                Content="&#xF602;"
                FontFamily="/Fonts/#FluentSystemIcons-Regular"
                FontSize="14"
                Foreground="{DynamicResource Foreground1}"
                RenderOptions.EdgeMode="Aliased"
                Style="{StaticResource TitleBarButtonStyle}">
                <Button.ToolTip>
                    <ToolTip x:Name="PinTooltip" Content="{x:Static lang:Resources.Pin}" />
                </Button.ToolTip>
            </Button>
        </StackPanel>
    </controls:MicaWindow.TitleBarContent>

    <Grid>
        <tb:TaskbarIcon
            x:Name="myNotifyIcon"
            IconSource="pack://application:,,,/Images/CPM.png"
            MenuActivation="LeftOrRightClick"
            PopupActivation="DoubleClick"
            ToolTipText="ColorPicker Max"
            Visibility="Visible">
            <tb:TaskbarIcon.ContextMenu>
                <ContextMenu>
                    <ContextMenu.Template>
                        <ControlTemplate>
                            <Border
                                Padding="5"
                                Background="{DynamicResource Background1}"
                                CornerRadius="5">
                                <ScrollViewer
                                    Uid="ScrollViewer_9"
                                    CanContentScroll="True"
                                    Style="{DynamicResource {ComponentResourceKey ResourceId=MenuScrollViewer,
                                                                                  TypeInTargetAssembly={x:Type FrameworkElement}}}">
                                    <ItemsPresenter
                                        Margin="{TemplateBinding Padding}"
                                        KeyboardNavigation.DirectionalNavigation="Cycle"
                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                </ScrollViewer>
                            </Border>
                        </ControlTemplate>
                    </ContextMenu.Template>
                    <MenuItem
                        x:Name="ShowMenu"
                        Click="ShowMenu_Click"
                        Foreground="{DynamicResource Foreground1}"
                        Header="{x:Static lang:Resources.Show}"
                        Style="{DynamicResource MenuStyle}" />
                    <MenuItem
                        x:Name="HideMenu"
                        Click="HideMenu_Click"
                        Foreground="{DynamicResource Foreground1}"
                        Header="{x:Static lang:Resources.Hide}"
                        Style="{DynamicResource MenuStyle}" />

                    <Separator Foreground="{DynamicResource Foreground1}" />
                    <MenuItem
                        x:Name="QuitMenu"
                        Click="QuitMenu_Click"
                        Foreground="{DynamicResource Foreground1}"
                        Header="{x:Static lang:Resources.Quit}"
                        Style="{DynamicResource MenuStyle}" />
                </ContextMenu>
            </tb:TaskbarIcon.ContextMenu>
        </tb:TaskbarIcon>

        <Grid x:Name="MainGrid">
            <Grid.RowDefinitions>
                <RowDefinition x:Name="GridRow1" Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Row 0: Header (Title / GridSplitter / Nav bar) -->
            <Grid Grid.Row="0" Margin="10 10 10 0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="100" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition x:Name="NavBarCol" Width="200" />
                </Grid.ColumnDefinitions>

                <!-- Col 0: Title block -->
                <StackPanel Grid.Column="0" VerticalAlignment="Top">
                    <TextBlock
                        FontSize="36"
                        FontWeight="ExtraBold"
                        Text="{x:Static lang:Resources.ColorPicker}" />
                    <TextBlock
                        x:Name="HelloTxt"
                        FontSize="16"
                        FontWeight="ExtraBold"
                        Text="{x:Static lang:Resources.ColorPicker}" />
                </StackPanel>

                <!-- Col 1: Resizable spacer (GridSplitter) -->
                <GridSplitter
                    Grid.Column="1"
                    Width="5"
                    Margin="5 0"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    Background="Transparent"
                    ResizeBehavior="PreviousAndNext"
                    ResizeDirection="Columns" />

                <!-- Col 2: New nav bar - 200px, 2 rows, flat (no border) -->
                <Grid
                    x:Name="NavBar"
                    Grid.Column="2"
                    VerticalAlignment="Top">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Row 0: 主题切换 / 主页 / 书签 / 设置 (icon-only) -->
                    <StackPanel
                        Grid.Row="0"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">
                        <Button
                            x:Name="ThemeToggleBtn"
                            Margin="0 0 5 0"
                            Click="ThemeToggleBtn_Click"
                            Style="{StaticResource NavActionButtonStyle}">
                            <Button.ToolTip>
                                <ToolTip Content="{x:Static lang:Resources.Theme}" />
                            </Button.ToolTip>
                            <Grid Width="18" Height="18">
                                <Viewbox
                                    x:Name="SunIconViewbox"
                                    Width="18"
                                    Height="18"
                                    Visibility="Visible">
                                    <Canvas Width="16" Height="16">
                                        <Path
                                            Fill="{DynamicResource AccentColor}"
                                            Data="M8 1a.75.75 0 0 1 .75.75v1.5a.75.75 0 0 1-1.5 0v-1.5A.75.75 0 0 1 8 1m2.5 7a2.5 2.5 0 1 1-5 0a2.5 2.5 0 0 1 5 0m2.45-3.89a.75.75 0 1 0-1.06-1.06l-1.062 1.06a.75.75 0 0 0 1.061 1.062zM15 8a.75.75 0 0 1-.75.75h-1.5a.75.75 0 0 1 0-1.5h1.5A.75.75 0 0 1 15 8m-3.11 4.95a.75.75 0 0 0 1.06-1.06l-1.06-1.062a.75.75 0 0 0-1.062 1.061zM8 12a.75.75 0 0 1 .75.75v1.5a.75.75 0 0 1-1.5 0v-1.5A.75.75 0 0 1 8 12m-2.828-.11a.75.75 0 0 0-1.061-1.062L3.05 11.89a.75.75 0 1 0 1.06 1.06zM4 8a.75.75 0 0 1-.75.75h-1.5a.75.75 0 0 1 0-1.5h1.5A.75.75 0 0 1 4 8m.11-2.828A.75.75 0 0 0 5.173 4.11L4.11 3.05a.75.75 0 1 0-1.06 1.06z" />
                                    </Canvas>
                                </Viewbox>
                                <Viewbox
                                    x:Name="MoonIconViewbox"
                                    Width="18"
                                    Height="18"
                                    Visibility="Collapsed">
                                    <Canvas Width="24" Height="24">
                                        <Path
                                            Fill="{DynamicResource AccentColor}"
                                            Data="M12.2 22c4.53 0 8.45-2.91 9.76-7.24c.11-.35.01-.74-.25-1s-.64-.36-1-.25c-.78.23-1.58.35-2.38.35c-4.52 0-8.2-3.68-8.2-8.2c0-.8.12-1.6.35-2.38a1.002 1.002 0 0 0-1.25-1.25A10.17 10.17 0 0 0 2 11.8C2 17.42 6.58 22 12.2 22" />
                                    </Canvas>
                                </Viewbox>
                            </Grid>
                        </Button>
                        <RadioButton
                            x:Name="HomePageBtn"
                            Margin="0 0 5 0"
                            Click="HomePageBtn_Click"
                            GroupName="NavBar"
                            Style="{StaticResource NavIconButtonStyle}">
                            <RadioButton.ToolTip>
                                <ToolTip Content="{x:Static lang:Resources.Home}" />
                            </RadioButton.ToolTip>
                            <TextBlock
                                FontFamily="/Fonts/#FluentSystemIcons-Filled"
                                FontSize="18"
                                Text="&#xF488;" />
                        </RadioButton>
                        <RadioButton
                            x:Name="BookmarksPageBtn"
                            Margin="0 0 5 0"
                            Click="BookmarksPageBtn_Click"
                            GroupName="NavBar"
                            Style="{StaticResource NavIconButtonStyle}">
                            <RadioButton.ToolTip>
                                <ToolTip Content="{x:Static lang:Resources.Bookmarks}" />
                            </RadioButton.ToolTip>
                            <TextBlock
                                FontFamily="/Fonts/#FluentSystemIcons-Filled"
                                FontSize="18"
                                Text="&#xF1F6;" />
                        </RadioButton>
                        <RadioButton
                            x:Name="SettingsPageBtn"
                            Click="SettingsPageBtn_Click"
                            GroupName="NavBar"
                            Style="{StaticResource NavIconButtonStyle}">
                            <RadioButton.ToolTip>
                                <ToolTip Content="{x:Static lang:Resources.Settings}" />
                            </RadioButton.ToolTip>
                            <TextBlock
                                FontFamily="/Fonts/#FluentSystemIcons-Filled"
                                FontSize="18"
                                Text="&#xF6B3;" />
                        </RadioButton>
                    </StackPanel>

                    <!-- Row 1: 选择颜色 / 获取对比度 / 生成调色板 (icon-only, 2-line tooltip) -->
                    <StackPanel
                        Grid.Row="1"
                        Margin="0 8 0 0"
                        HorizontalAlignment="Right"
                        Orientation="Horizontal">
                        <Button
                            x:Name="NavSelectColorBtn"
                            Margin="0 0 5 0"
                            Click="NavSelectColorBtn_Click"
                            Style="{StaticResource NavActionButtonStyle}">
                            <Button.ToolTip>
                                <ToolTip>
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" Text="{x:Static lang:Resources.SelectColor}" />
                                        <TextBlock x:Name="NavSelectColorShortcutTxt" Text="Ctrl+D2" />
                                    </StackPanel>
                                </ToolTip>
                            </Button.ToolTip>
                            <TextBlock
                                FontFamily="/Fonts/#FluentSystemIcons-Filled"
                                FontSize="18"
                                Text="&#xFD48;" />
                        </Button>
                        <Button
                            x:Name="NavContrastBtn"
                            Margin="0 0 5 0"
                            Click="NavContrastBtn_Click"
                            Style="{StaticResource NavActionButtonStyle}">
                            <Button.ToolTip>
                                <ToolTip>
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" Text="{x:Static lang:Resources.GetContrast}" />
                                        <TextBlock Text="Alt+A" />
                                    </StackPanel>
                                </ToolTip>
                            </Button.ToolTip>
                            <TextBlock
                                FontFamily="/Fonts/#FluentSystemIcons-Filled"
                                FontSize="18"
                                Text="&#xFD1B;" />
                        </Button>
                        <Button
                            x:Name="NavPaletteBtn"
                            Click="NavPaletteBtn_Click"
                            Style="{StaticResource NavActionButtonStyle}">
                            <Button.ToolTip>
                                <ToolTip>
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" Text="{x:Static lang:Resources.GeneratePalette}" />
                                        <TextBlock Text="Alt+P" />
                                    </StackPanel>
                                </ToolTip>
                            </Button.ToolTip>
                            <TextBlock
                                FontFamily="/Fonts/#FluentSystemIcons-Filled"
                                FontSize="18"
                                Text="&#xF2F6;" />
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>

            <!-- Row 1: Main content (full width since sidebar is hidden) -->
            <ScrollViewer
                x:Name="PageScroller"
                Grid.Row="1"
                Margin="10 10 10 0"
                HorizontalScrollBarVisibility="Disabled"
                Style="{DynamicResource ScrollViewerStyle}"
                VerticalScrollBarVisibility="Auto">
                <Frame x:Name="PageDisplayer" NavigationUIVisibility="Hidden" />
            </ScrollViewer>

            <!-- Old sidebar preserved (Collapsed) for future restoration -->
            <StackPanel
                x:Name="SideBar"
                Grid.Row="1"
                Visibility="Collapsed" />
        </Grid>
    </Grid>
</controls:MicaWindow>