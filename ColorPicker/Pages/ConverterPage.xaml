<Page x:Class="ColorPicker.Pages.ConverterPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ColorPicker.Pages"
      xmlns:lang="clr-namespace:ColorPicker.Properties"
	  mc:Ignorable="d" 
	  Foreground="{Binding Source={StaticResource Foreground1}}"
	  d:DesignHeight="350" d:DesignWidth="560"
	  FontFamily="../Fonts/#Montserrat"
      Title="ConverterPage">

    <Grid>
		<Grid>
			<Grid.RowDefinitions>
				<RowDefinition/>
				<RowDefinition Height="Auto"/>
			</Grid.RowDefinitions>

			<StackPanel Margin="10,40,10,5" Grid.Row="0">
				<TextBlock Text="{x:Static lang:Resources.Converter}" Foreground="{Binding Source={StaticResource Foreground1}}" FontSize="16"/>
				<Grid>
					<Grid.RowDefinitions>
						<RowDefinition Height="Auto"/>
						<RowDefinition/>
					</Grid.RowDefinitions>

					<StackPanel Grid.Row="0" Orientation="Horizontal">
						<TextBlock FontWeight="Bold" FontSize="14" Text="{x:Static lang:Resources.ColorType}"/>
						<ComboBox x:Name="ColorTypeComboBox" Foreground="{Binding Source={StaticResource Foreground1}}" Style="{DynamicResource ComboBoxStyle1}" Margin="10,0,0,0" BorderThickness="2" Padding="5"/>
						<TextBox x:Name="ColorTxt" TextChanged="ColorTxt_TextChanged" Width="150" Style="{DynamicResource TextBoxStyle1}" BorderBrush="{Binding Source={StaticResource AccentColor}}" CaretBrush="{Binding Source={StaticResource Foreground1}}" BorderThickness="2" Margin="10,0,0,0"/>
					</StackPanel>

					<StackPanel Grid.Row="1" Margin="0,10,0,0">
						<TextBlock FontWeight="Bold" FontSize="14" Text="{x:Static lang:Resources.RGB}" x:Name="RGBTxt"/>
						<TextBlock FontWeight="Bold" FontSize="14" Text="{x:Static lang:Resources.HEX}" x:Name="HEXTxt"/>
					</StackPanel>
				</Grid>
			</StackPanel>
		</Grid>
	</Grid>
</Page>
